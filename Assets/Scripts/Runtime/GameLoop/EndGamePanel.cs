using UnityEngine;

namespace GiftOrCoal.Timer
{
    public sealed class EndGamePanel : MonoBehaviour
    {
        public void Enable()
        {
            gameObject.SetActive(true);
        }
    }
}